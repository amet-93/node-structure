const register = (req, res) => {
    console.log("yes")
    // Auth.register(req.body, (err, success) => {
    //   if (err) {
    //     res.status(400).send(err);
    //   } else {
    //     res.send(success);
    //   }
    // });
  };

  module.exports = {
    register
  }